- name: Insert the F5 Demo project
  uri:
    url: https://localhost/api/v2/projects/
    validate_certs: no
    method: POST
    user: "{{ tower_user }}"
    password: "{{ tower_pass }}"
    body_format: json
    force_basic_auth: yes
    status_code: 201
    body: |


      {
          "name": "F5 Lab Demo",
          "description": "",
          "scm_type": "git",
          "scm_url": "{{ this_git_url }}", 
          "scm_branch": "",
          "scm_refspec": "",
          "scm_clean": true,
          "scm_delete_on_update": true,
          "credential": null,
          "timeout": 0,
          "organization": 1,
          "scm_update_on_launch": true,
          "scm_update_cache_timeout": 0,
          "allow_override": false,
          "custom_virtualenv": null
      }
  ignore_errors: yes
  no_log: True
  failed_when: False
  changed_when: False
  tags:
  - projects
  - add_stuff

## If the project already existed, the output is different from the input. Instead we query again and parse the ID here.
- name: Get ID from F5 Lab Demo project ID
  uri:
    url: https://localhost/api/v2/projects/?name=F5%20Lab%20Demo
    validate_certs: no
    method: GET
    user: "{{ tower_user }}"
    password: "{{ tower_pass }}"
    body_format: json
    force_basic_auth: yes
  register: project_insert
  tags:
  - projects
  - add_stuff

- set_fact:
    new_project_id: "{{ project_insert.json.results[0].id }}"
  tags:
  - projects
  - add_stuff
